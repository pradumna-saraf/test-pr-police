name: Agent PR Police
on:
  pull_request:
permissions:
  contents: read
  pull-requests: write
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: Pradumnasaraf/agent-pr-police@v1
        with:
          fail-on: high
